# PyTorch wheels for NVIDIA GPUs (CUDA 12.4) on Windows/Linux.
# Use the PyTorch index only as an additional source for the exact CUDA wheel.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.0+cu124

-r requirements.txt
