datasets==4.4.0
pandas==2.3.3
jsonlines==4.0.0
tqdm==4.67.1

# PDF extraction and corpus construction.
pymupdf==1.26.6
pdfplumber==0.11.7

# Synthetic QA generation and model-output evaluation.
openai>=1.68.0
pydantic>=2.7.0
ragas==0.4.3
# Ragas 0.4.3 still imports legacy langchain_community.chat_models.vertexai.
# langchain-community 0.4.x removed that module, so keep this 0.3.x pin.
langchain-community==0.3.31
langchain-openai>=1.1.0
python-dotenv>=1.2.0
tiktoken>=0.12.0
numpy>=1.26.0
rank-bm25==0.2.2
sentence-transformers==3.4.1
lancedb==0.29.0

# Fine-tuning and inference stack (TRL + PEFT + QLoRA).
# For CUDA workstations, install the PyTorch wheel that matches the local CUDA
# driver first if needed, then install this file.
torch>=2.5.0
transformers>=4.57.0
accelerate>=1.11.0
bitsandbytes>=0.48.0
peft>=0.17.0
trl>=0.24.0
huggingface_hub>=0.36.0
sentencepiece>=0.2.0
protobuf>=5.27.0
tensorboard>=2.18.0
litellm
